apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: db-creds
  namespace: demo
spec:
  refreshInterval: 1m
  secretStoreRef:
    name: aws-store
    kind: SecretStore
  target:
    name: db-secret # Tên K8s Secret sẽ được sinh ra tự động
  data:
    - secretKey: password
      remoteRef:
        key: demo/db/password # Đường dẫn secret trên AWS Secrets Manager
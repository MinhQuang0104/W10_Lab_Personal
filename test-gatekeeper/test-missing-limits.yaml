apiVersion: v1
kind: Pod
metadata:
  name: test-missing-limits
  namespace: default
spec:
  containers:
  - name: nginx
    image: nginx:1.25.1
    resources:
      requests:
        memory: "64Mi"
        cpu: "250m"
      # Thiếu hoàn toàn block limits ở đây
